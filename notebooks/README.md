# Notebooks

Exploration only. Nothing under `src/` imports from here, and no paper
number is ever sourced from a notebook — paper numbers come exclusively from
runner stages (`score`, `stats`, `figures`).
