# Integration tests

End-to-end smoke on the tinyset fixture (CPU, < 5 min) plus the determinism
check (two smoke runs, same seed, byte-identical `metrics.json`). Added once
the runner and simulator exist.
