# Golden metric fixtures

Tiny handcrafted emission logs + reference + hand-computed expected value
for every metric (YAML; the hand computation is documented inside each
fixture file). Every metric ships with at least three goldens: typical,
edge, and undefined/None case.
