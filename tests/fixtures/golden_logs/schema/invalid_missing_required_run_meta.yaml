# Rule: missing required fields fail validation.
# run_meta omits config_hash (top level) and wallclock_protocol.timer
# (nested) => two missing-required-field errors. Events are minimal and
# valid so the run_meta errors are isolated.
description: run_meta missing config_hash and wallclock_protocol.timer.
expect:
  valid: false
  error_codes: [missing-required-field]
  warning_codes: []
run_meta:
  schema_version: "1.0"
  run_id: "E1/chunked-k4-s42"
  created_utc: "2026-08-14T09:31:07Z"
  git_sha: "a1b2c3d"
  experiment: "e1_pareto"
  dataset: { name: phoenix14t, split: test, fps: 25.0, unit: gloss }
  model:
    name: transformer_ctc
    params: 9200000
    mask: { mode: chunked, chunk_frames: 4, lookahead_frames: 4 }
  seed: 42
  hardware: { gpu: "NVIDIA RTX 4090 24GB" }
  wallclock_protocol: { batch_size: 1, warmup_utts: 10 }
events:
  - { type: utt_start, utt_id: u1, n_frames: 10 }
  - { type: emission, utt_id: u1, t_frame: 9, t_wall_ms: 1.0, hyp: ["A"], is_final: true }
